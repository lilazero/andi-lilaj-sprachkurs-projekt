"use client";

import React from "react";

// Minimal placeholder ShoppingCart UI component
export const ShoppingCart = () => {
  return (
    <div>
      <h1>Shopping Cart</h1>
    </div>
  );
};

export default ShoppingCart;
